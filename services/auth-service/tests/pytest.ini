[pytest]
pythonpath = .
asyncio_mode = auto
addopts = --strict-markers -v -p no:cacheprovider
testpaths = .
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests
    slow: Slow running tests
    db: Tests requiring database
