# Allow build scripts for packages that ship native/postinstall binaries.
# When Dependabot bumps versions, update these pins to match.
only-built-packages[] = esbuild
only-built-packages[] = sharp
only-built-packages[] = unrs-resolver
